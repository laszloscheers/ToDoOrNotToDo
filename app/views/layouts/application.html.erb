<!DOCTYPE html>
<html>
  <head>
    <title>toDoOrNotToDo</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="description" content="Todoornottodo application allows you to create lists with tasks that may be arked as completed or not.">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", "data-turbolinks-eval": "false", defer: true %>
    
    <!-- Stylesheets for icons in the footer-->
    <%= stylesheet_link_tag "font-awesome.min" %>

  </head>

  <body>
  
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>

    <!-- Renders navbar from partials-->
    <%= render partial: 'partials/navbar' %>

    <% if notice %>
      <%= render 'partials/notice' %>
    <% end %>

    <% if alert %>
      <%= render 'partials/alert' %>
    <% end %>

    <%= yield %>

    <!-- Renders footer from partials-->
    <%= render 'partials/footer' %>
    
  </body>
</html>
